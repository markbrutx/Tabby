// Canonical definitions live in tabby-kernel.
// Re-exported here for backward compatibility.
pub use tabby_kernel::{
    BrowserUrl, CommandTemplate, LayoutPreset, PaneId, TabId, ValueObjectError, WorkingDirectory,
};
