[package]
name = "tabby-contracts"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
specta = { version = "=2.0.0-rc.22", features = ["derive"] }
tabby-kernel = { path = "../tabby-kernel" }
